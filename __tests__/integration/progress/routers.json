[
  {
    "router": "playerProgress",
    "data": {
      "id_chapter": "{{id_chapter}}",
      "id_user": "{{id_player}}"
    }
  },
  {
    "router": "chapterProgress",
    "data": {
      "id_player_progress": "{{id_playerProgress}}",
      "id_level": "{{id_level}}"
    }
  },
  {
    "router": "levelProgress",
    "data": {
      "id_chapter_progress": "{{id_chapterProgress}}",
      "id_orderLevel": "{{id_orderLevel}}"
    }
  }
]
